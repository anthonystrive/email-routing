{
  "name": "email-routing",
  "version": "1.0.0",
  "private": true,
  "description": "Email → PDF extraction → webhook, on Google Apps Script",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
